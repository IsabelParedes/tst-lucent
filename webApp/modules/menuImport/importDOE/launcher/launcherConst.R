launcherConst <- list(
        STATUS = list(
            ready = "ready", 
            configuring = "configuring", 
            configured = "configured", 
            waiting = "waiting", 
            running = "running", 
            loading = "loading", 
            ended = "ended", 
            onerror = "onerror",
            disabled = "disabled"
        ),
        RUN_ACTION = list(
            configureLaunchLoad = "ConfigureLaunchLoad", 
            configure = "Configure", 
            load = "Load",
            cancel = "Cancel",
            enDisable = "EnDisable"
        ),
        SIMU_INFO_JSON = "SimulationsLauncherInfo.json"
)
